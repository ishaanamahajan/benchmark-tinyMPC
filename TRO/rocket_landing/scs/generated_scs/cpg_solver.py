
"""
Auto-generated by CVXPYgen on July 23, 2025 at 16:07:47.
Content: Custom solve method for CVXPY interface.
"""

import time
import warnings
import numpy as np
from cvxpy.reductions import Solution
from cvxpy.problems.problem import SolverStats
from scs.generated_scs import cpg_module


standard_settings_names = {}


def squeeze_scalar(val):
    if isinstance(val, np.ndarray):
        val = val.squeeze()
        if val.shape == ():
            return val.item()
    return val


def cpg_solve(prob, updated_params=None, **kwargs):

    # set flags for updated parameters
    upd = cpg_module.cpg_updated()
    if updated_params is None:
        updated_params = ["param3", "param1"]
    for p in updated_params:
        try:
            setattr(upd, p, True)
        except AttributeError:
            raise AttributeError(f"{p} is not a parameter.")

    # set solver settings
    cpg_module.set_solver_default_settings()
    for key, value in kwargs.items():
        try:
            eval(f'cpg_module.set_solver_{standard_settings_names.get(key, key)}(value)')
        except AttributeError:
            raise AttributeError(f'Solver setting "{key}" not available.')

    # set parameter values
    par = cpg_module.cpg_params()
    param_dict = prob.param_dict
    par.param3 = list(param_dict["param3"].value.flatten(order="F"))
    par.param1 = list(param_dict["param1"].value.flatten(order="F"))

    # solve
    t0 = time.time()
    res = cpg_module.solve(upd, par)
    t1 = time.time()

    # store solution in problem object
    prob._clear_solution()
    prob.var_dict['var2'].save_value(np.array(res.cpg_prim.var2).reshape((573,), order='F'))
    prob.constraints[0].save_dual_value(np.array(res.cpg_dual.d0).reshape((6,), order='F'))
    prob.constraints[1].save_dual_value(np.array(res.cpg_dual.d1).reshape((6,), order='F'))
    prob.constraints[2].save_dual_value(np.array(res.cpg_dual.d2).reshape((6,), order='F'))
    prob.constraints[3].save_dual_value(np.array(res.cpg_dual.d3).reshape((6,), order='F'))
    prob.constraints[4].save_dual_value(np.array(res.cpg_dual.d4).reshape((6,), order='F'))
    prob.constraints[5].save_dual_value(np.array(res.cpg_dual.d5).reshape((6,), order='F'))
    prob.constraints[6].save_dual_value(np.array(res.cpg_dual.d6).reshape((6,), order='F'))
    prob.constraints[7].save_dual_value(np.array(res.cpg_dual.d7).reshape((6,), order='F'))
    prob.constraints[8].save_dual_value(np.array(res.cpg_dual.d8).reshape((6,), order='F'))
    prob.constraints[9].save_dual_value(np.array(res.cpg_dual.d9).reshape((6,), order='F'))
    prob.constraints[10].save_dual_value(np.array(res.cpg_dual.d10).reshape((6,), order='F'))
    prob.constraints[11].save_dual_value(np.array(res.cpg_dual.d11).reshape((6,), order='F'))
    prob.constraints[12].save_dual_value(np.array(res.cpg_dual.d12).reshape((6,), order='F'))
    prob.constraints[13].save_dual_value(np.array(res.cpg_dual.d13).reshape((6,), order='F'))
    prob.constraints[14].save_dual_value(np.array(res.cpg_dual.d14).reshape((6,), order='F'))
    prob.constraints[15].save_dual_value(np.array(res.cpg_dual.d15).reshape((6,), order='F'))
    prob.constraints[16].save_dual_value(np.array(res.cpg_dual.d16).reshape((6,), order='F'))
    prob.constraints[17].save_dual_value(np.array(res.cpg_dual.d17).reshape((6,), order='F'))
    prob.constraints[18].save_dual_value(np.array(res.cpg_dual.d18).reshape((6,), order='F'))
    prob.constraints[19].save_dual_value(np.array(res.cpg_dual.d19).reshape((6,), order='F'))
    prob.constraints[20].save_dual_value(np.array(res.cpg_dual.d20).reshape((6,), order='F'))
    prob.constraints[21].save_dual_value(np.array(res.cpg_dual.d21).reshape((6,), order='F'))
    prob.constraints[22].save_dual_value(np.array(res.cpg_dual.d22).reshape((6,), order='F'))
    prob.constraints[23].save_dual_value(np.array(res.cpg_dual.d23).reshape((6,), order='F'))
    prob.constraints[24].save_dual_value(np.array(res.cpg_dual.d24).reshape((6,), order='F'))
    prob.constraints[25].save_dual_value(np.array(res.cpg_dual.d25).reshape((6,), order='F'))
    prob.constraints[26].save_dual_value(np.array(res.cpg_dual.d26).reshape((6,), order='F'))
    prob.constraints[27].save_dual_value(np.array(res.cpg_dual.d27).reshape((6,), order='F'))
    prob.constraints[28].save_dual_value(np.array(res.cpg_dual.d28).reshape((6,), order='F'))
    prob.constraints[29].save_dual_value(np.array(res.cpg_dual.d29).reshape((6,), order='F'))
    prob.constraints[30].save_dual_value(np.array(res.cpg_dual.d30).reshape((6,), order='F'))
    prob.constraints[31].save_dual_value(np.array(res.cpg_dual.d31).reshape((6,), order='F'))
    prob.constraints[32].save_dual_value(np.array(res.cpg_dual.d32).reshape((6,), order='F'))
    prob.constraints[33].save_dual_value(np.array(res.cpg_dual.d33).reshape((6,), order='F'))
    prob.constraints[34].save_dual_value(np.array(res.cpg_dual.d34).reshape((6,), order='F'))
    prob.constraints[35].save_dual_value(np.array(res.cpg_dual.d35).reshape((6,), order='F'))
    prob.constraints[36].save_dual_value(np.array(res.cpg_dual.d36).reshape((6,), order='F'))
    prob.constraints[37].save_dual_value(np.array(res.cpg_dual.d37).reshape((6,), order='F'))
    prob.constraints[38].save_dual_value(np.array(res.cpg_dual.d38).reshape((6,), order='F'))
    prob.constraints[39].save_dual_value(np.array(res.cpg_dual.d39).reshape((6,), order='F'))
    prob.constraints[40].save_dual_value(np.array(res.cpg_dual.d40).reshape((6,), order='F'))
    prob.constraints[41].save_dual_value(np.array(res.cpg_dual.d41).reshape((6,), order='F'))
    prob.constraints[42].save_dual_value(np.array(res.cpg_dual.d42).reshape((6,), order='F'))
    prob.constraints[43].save_dual_value(np.array(res.cpg_dual.d43).reshape((6,), order='F'))
    prob.constraints[44].save_dual_value(np.array(res.cpg_dual.d44).reshape((6,), order='F'))
    prob.constraints[45].save_dual_value(np.array(res.cpg_dual.d45).reshape((6,), order='F'))
    prob.constraints[46].save_dual_value(np.array(res.cpg_dual.d46).reshape((6,), order='F'))
    prob.constraints[47].save_dual_value(np.array(res.cpg_dual.d47).reshape((6,), order='F'))
    prob.constraints[48].save_dual_value(np.array(res.cpg_dual.d48).reshape((6,), order='F'))
    prob.constraints[49].save_dual_value(np.array(res.cpg_dual.d49).reshape((6,), order='F'))
    prob.constraints[50].save_dual_value(np.array(res.cpg_dual.d50).reshape((6,), order='F'))
    prob.constraints[51].save_dual_value(np.array(res.cpg_dual.d51).reshape((6,), order='F'))
    prob.constraints[52].save_dual_value(np.array(res.cpg_dual.d52).reshape((6,), order='F'))
    prob.constraints[53].save_dual_value(np.array(res.cpg_dual.d53).reshape((6,), order='F'))
    prob.constraints[54].save_dual_value(np.array(res.cpg_dual.d54).reshape((6,), order='F'))
    prob.constraints[55].save_dual_value(np.array(res.cpg_dual.d55).reshape((6,), order='F'))
    prob.constraints[56].save_dual_value(np.array(res.cpg_dual.d56).reshape((6,), order='F'))
    prob.constraints[57].save_dual_value(np.array(res.cpg_dual.d57).reshape((6,), order='F'))
    prob.constraints[58].save_dual_value(np.array(res.cpg_dual.d58).reshape((6,), order='F'))
    prob.constraints[59].save_dual_value(np.array(res.cpg_dual.d59).reshape((6,), order='F'))
    prob.constraints[60].save_dual_value(np.array(res.cpg_dual.d60).reshape((6,), order='F'))
    prob.constraints[61].save_dual_value(np.array(res.cpg_dual.d61).reshape((6,), order='F'))
    prob.constraints[62].save_dual_value(np.array(res.cpg_dual.d62).reshape((6,), order='F'))
    prob.constraints[63].save_dual_value(np.array(res.cpg_dual.d63).reshape((6,), order='F'))
    prob.constraints[64].save_dual_value(np.array(res.cpg_dual.d64))
    prob.constraints[65].save_dual_value(np.array(res.cpg_dual.d65))
    prob.constraints[66].save_dual_value(np.array(res.cpg_dual.d66))
    prob.constraints[67].save_dual_value(np.array(res.cpg_dual.d67))
    prob.constraints[68].save_dual_value(np.array(res.cpg_dual.d68))
    prob.constraints[69].save_dual_value(np.array(res.cpg_dual.d69))
    prob.constraints[70].save_dual_value(np.array(res.cpg_dual.d70))
    prob.constraints[71].save_dual_value(np.array(res.cpg_dual.d71))
    prob.constraints[72].save_dual_value(np.array(res.cpg_dual.d72))
    prob.constraints[73].save_dual_value(np.array(res.cpg_dual.d73))
    prob.constraints[74].save_dual_value(np.array(res.cpg_dual.d74))
    prob.constraints[75].save_dual_value(np.array(res.cpg_dual.d75))
    prob.constraints[76].save_dual_value(np.array(res.cpg_dual.d76))
    prob.constraints[77].save_dual_value(np.array(res.cpg_dual.d77))
    prob.constraints[78].save_dual_value(np.array(res.cpg_dual.d78))
    prob.constraints[79].save_dual_value(np.array(res.cpg_dual.d79))
    prob.constraints[80].save_dual_value(np.array(res.cpg_dual.d80))
    prob.constraints[81].save_dual_value(np.array(res.cpg_dual.d81))
    prob.constraints[82].save_dual_value(np.array(res.cpg_dual.d82))
    prob.constraints[83].save_dual_value(np.array(res.cpg_dual.d83))
    prob.constraints[84].save_dual_value(np.array(res.cpg_dual.d84))
    prob.constraints[85].save_dual_value(np.array(res.cpg_dual.d85))
    prob.constraints[86].save_dual_value(np.array(res.cpg_dual.d86))
    prob.constraints[87].save_dual_value(np.array(res.cpg_dual.d87))
    prob.constraints[88].save_dual_value(np.array(res.cpg_dual.d88))
    prob.constraints[89].save_dual_value(np.array(res.cpg_dual.d89))
    prob.constraints[90].save_dual_value(np.array(res.cpg_dual.d90))
    prob.constraints[91].save_dual_value(np.array(res.cpg_dual.d91))
    prob.constraints[92].save_dual_value(np.array(res.cpg_dual.d92))
    prob.constraints[93].save_dual_value(np.array(res.cpg_dual.d93))
    prob.constraints[94].save_dual_value(np.array(res.cpg_dual.d94))
    prob.constraints[95].save_dual_value(np.array(res.cpg_dual.d95))
    prob.constraints[96].save_dual_value(np.array(res.cpg_dual.d96))
    prob.constraints[97].save_dual_value(np.array(res.cpg_dual.d97))
    prob.constraints[98].save_dual_value(np.array(res.cpg_dual.d98))
    prob.constraints[99].save_dual_value(np.array(res.cpg_dual.d99))
    prob.constraints[100].save_dual_value(np.array(res.cpg_dual.d100))
    prob.constraints[101].save_dual_value(np.array(res.cpg_dual.d101))
    prob.constraints[102].save_dual_value(np.array(res.cpg_dual.d102))
    prob.constraints[103].save_dual_value(np.array(res.cpg_dual.d103))
    prob.constraints[104].save_dual_value(np.array(res.cpg_dual.d104))
    prob.constraints[105].save_dual_value(np.array(res.cpg_dual.d105))
    prob.constraints[106].save_dual_value(np.array(res.cpg_dual.d106))
    prob.constraints[107].save_dual_value(np.array(res.cpg_dual.d107))
    prob.constraints[108].save_dual_value(np.array(res.cpg_dual.d108))
    prob.constraints[109].save_dual_value(np.array(res.cpg_dual.d109))
    prob.constraints[110].save_dual_value(np.array(res.cpg_dual.d110))
    prob.constraints[111].save_dual_value(np.array(res.cpg_dual.d111))
    prob.constraints[112].save_dual_value(np.array(res.cpg_dual.d112))
    prob.constraints[113].save_dual_value(np.array(res.cpg_dual.d113))
    prob.constraints[114].save_dual_value(np.array(res.cpg_dual.d114))
    prob.constraints[115].save_dual_value(np.array(res.cpg_dual.d115))
    prob.constraints[116].save_dual_value(np.array(res.cpg_dual.d116))
    prob.constraints[117].save_dual_value(np.array(res.cpg_dual.d117))
    prob.constraints[118].save_dual_value(np.array(res.cpg_dual.d118))
    prob.constraints[119].save_dual_value(np.array(res.cpg_dual.d119))
    prob.constraints[120].save_dual_value(np.array(res.cpg_dual.d120))
    prob.constraints[121].save_dual_value(np.array(res.cpg_dual.d121))
    prob.constraints[122].save_dual_value(np.array(res.cpg_dual.d122))
    prob.constraints[123].save_dual_value(np.array(res.cpg_dual.d123))
    prob.constraints[124].save_dual_value(np.array(res.cpg_dual.d124))
    prob.constraints[125].save_dual_value(np.array(res.cpg_dual.d125))
    prob.constraints[126].save_dual_value(np.array(res.cpg_dual.d126))
    prob.constraints[127].save_dual_value(np.array(res.cpg_dual.d127).reshape((3,), order='F'))
    prob.constraints[128].save_dual_value(np.array(res.cpg_dual.d128).reshape((3,), order='F'))
    prob.constraints[129].save_dual_value(np.array(res.cpg_dual.d129).reshape((3,), order='F'))
    prob.constraints[130].save_dual_value(np.array(res.cpg_dual.d130).reshape((3,), order='F'))
    prob.constraints[131].save_dual_value(np.array(res.cpg_dual.d131).reshape((3,), order='F'))
    prob.constraints[132].save_dual_value(np.array(res.cpg_dual.d132).reshape((3,), order='F'))
    prob.constraints[133].save_dual_value(np.array(res.cpg_dual.d133).reshape((3,), order='F'))
    prob.constraints[134].save_dual_value(np.array(res.cpg_dual.d134).reshape((3,), order='F'))
    prob.constraints[135].save_dual_value(np.array(res.cpg_dual.d135).reshape((3,), order='F'))
    prob.constraints[136].save_dual_value(np.array(res.cpg_dual.d136).reshape((3,), order='F'))
    prob.constraints[137].save_dual_value(np.array(res.cpg_dual.d137).reshape((3,), order='F'))
    prob.constraints[138].save_dual_value(np.array(res.cpg_dual.d138).reshape((3,), order='F'))
    prob.constraints[139].save_dual_value(np.array(res.cpg_dual.d139).reshape((3,), order='F'))
    prob.constraints[140].save_dual_value(np.array(res.cpg_dual.d140).reshape((3,), order='F'))
    prob.constraints[141].save_dual_value(np.array(res.cpg_dual.d141).reshape((3,), order='F'))
    prob.constraints[142].save_dual_value(np.array(res.cpg_dual.d142).reshape((3,), order='F'))
    prob.constraints[143].save_dual_value(np.array(res.cpg_dual.d143).reshape((3,), order='F'))
    prob.constraints[144].save_dual_value(np.array(res.cpg_dual.d144).reshape((3,), order='F'))
    prob.constraints[145].save_dual_value(np.array(res.cpg_dual.d145).reshape((3,), order='F'))
    prob.constraints[146].save_dual_value(np.array(res.cpg_dual.d146).reshape((3,), order='F'))
    prob.constraints[147].save_dual_value(np.array(res.cpg_dual.d147).reshape((3,), order='F'))
    prob.constraints[148].save_dual_value(np.array(res.cpg_dual.d148).reshape((3,), order='F'))
    prob.constraints[149].save_dual_value(np.array(res.cpg_dual.d149).reshape((3,), order='F'))
    prob.constraints[150].save_dual_value(np.array(res.cpg_dual.d150).reshape((3,), order='F'))
    prob.constraints[151].save_dual_value(np.array(res.cpg_dual.d151).reshape((3,), order='F'))
    prob.constraints[152].save_dual_value(np.array(res.cpg_dual.d152).reshape((3,), order='F'))
    prob.constraints[153].save_dual_value(np.array(res.cpg_dual.d153).reshape((3,), order='F'))
    prob.constraints[154].save_dual_value(np.array(res.cpg_dual.d154).reshape((3,), order='F'))
    prob.constraints[155].save_dual_value(np.array(res.cpg_dual.d155).reshape((3,), order='F'))
    prob.constraints[156].save_dual_value(np.array(res.cpg_dual.d156).reshape((3,), order='F'))
    prob.constraints[157].save_dual_value(np.array(res.cpg_dual.d157).reshape((3,), order='F'))
    prob.constraints[158].save_dual_value(np.array(res.cpg_dual.d158).reshape((3,), order='F'))
    prob.constraints[159].save_dual_value(np.array(res.cpg_dual.d159).reshape((3,), order='F'))
    prob.constraints[160].save_dual_value(np.array(res.cpg_dual.d160).reshape((3,), order='F'))
    prob.constraints[161].save_dual_value(np.array(res.cpg_dual.d161).reshape((3,), order='F'))
    prob.constraints[162].save_dual_value(np.array(res.cpg_dual.d162).reshape((3,), order='F'))
    prob.constraints[163].save_dual_value(np.array(res.cpg_dual.d163).reshape((3,), order='F'))
    prob.constraints[164].save_dual_value(np.array(res.cpg_dual.d164).reshape((3,), order='F'))
    prob.constraints[165].save_dual_value(np.array(res.cpg_dual.d165).reshape((3,), order='F'))
    prob.constraints[166].save_dual_value(np.array(res.cpg_dual.d166).reshape((3,), order='F'))
    prob.constraints[167].save_dual_value(np.array(res.cpg_dual.d167).reshape((3,), order='F'))
    prob.constraints[168].save_dual_value(np.array(res.cpg_dual.d168).reshape((3,), order='F'))
    prob.constraints[169].save_dual_value(np.array(res.cpg_dual.d169).reshape((3,), order='F'))
    prob.constraints[170].save_dual_value(np.array(res.cpg_dual.d170).reshape((3,), order='F'))
    prob.constraints[171].save_dual_value(np.array(res.cpg_dual.d171).reshape((3,), order='F'))
    prob.constraints[172].save_dual_value(np.array(res.cpg_dual.d172).reshape((3,), order='F'))
    prob.constraints[173].save_dual_value(np.array(res.cpg_dual.d173).reshape((3,), order='F'))
    prob.constraints[174].save_dual_value(np.array(res.cpg_dual.d174).reshape((3,), order='F'))
    prob.constraints[175].save_dual_value(np.array(res.cpg_dual.d175).reshape((3,), order='F'))
    prob.constraints[176].save_dual_value(np.array(res.cpg_dual.d176).reshape((3,), order='F'))
    prob.constraints[177].save_dual_value(np.array(res.cpg_dual.d177).reshape((3,), order='F'))
    prob.constraints[178].save_dual_value(np.array(res.cpg_dual.d178).reshape((3,), order='F'))
    prob.constraints[179].save_dual_value(np.array(res.cpg_dual.d179).reshape((3,), order='F'))
    prob.constraints[180].save_dual_value(np.array(res.cpg_dual.d180).reshape((3,), order='F'))
    prob.constraints[181].save_dual_value(np.array(res.cpg_dual.d181).reshape((3,), order='F'))
    prob.constraints[182].save_dual_value(np.array(res.cpg_dual.d182).reshape((3,), order='F'))
    prob.constraints[183].save_dual_value(np.array(res.cpg_dual.d183).reshape((3,), order='F'))
    prob.constraints[184].save_dual_value(np.array(res.cpg_dual.d184).reshape((3,), order='F'))
    prob.constraints[185].save_dual_value(np.array(res.cpg_dual.d185).reshape((3,), order='F'))
    prob.constraints[186].save_dual_value(np.array(res.cpg_dual.d186).reshape((3,), order='F'))
    prob.constraints[187].save_dual_value(np.array(res.cpg_dual.d187).reshape((3,), order='F'))
    prob.constraints[188].save_dual_value(np.array(res.cpg_dual.d188).reshape((3,), order='F'))
    prob.constraints[189].save_dual_value(np.array(res.cpg_dual.d189).reshape((3,), order='F'))
    prob.constraints[190].save_dual_value(np.array(res.cpg_dual.d190).reshape((3,), order='F'))
    prob.constraints[191].save_dual_value(np.array(res.cpg_dual.d191).reshape((3,), order='F'))
    prob.constraints[192].save_dual_value(np.array(res.cpg_dual.d192).reshape((3,), order='F'))
    prob.constraints[193].save_dual_value(np.array(res.cpg_dual.d193).reshape((3,), order='F'))
    prob.constraints[194].save_dual_value(np.array(res.cpg_dual.d194).reshape((3,), order='F'))
    prob.constraints[195].save_dual_value(np.array(res.cpg_dual.d195).reshape((3,), order='F'))
    prob.constraints[196].save_dual_value(np.array(res.cpg_dual.d196).reshape((3,), order='F'))
    prob.constraints[197].save_dual_value(np.array(res.cpg_dual.d197).reshape((3,), order='F'))
    prob.constraints[198].save_dual_value(np.array(res.cpg_dual.d198).reshape((3,), order='F'))
    prob.constraints[199].save_dual_value(np.array(res.cpg_dual.d199).reshape((3,), order='F'))
    prob.constraints[200].save_dual_value(np.array(res.cpg_dual.d200).reshape((3,), order='F'))
    prob.constraints[201].save_dual_value(np.array(res.cpg_dual.d201).reshape((3,), order='F'))
    prob.constraints[202].save_dual_value(np.array(res.cpg_dual.d202).reshape((3,), order='F'))
    prob.constraints[203].save_dual_value(np.array(res.cpg_dual.d203).reshape((3,), order='F'))
    prob.constraints[204].save_dual_value(np.array(res.cpg_dual.d204).reshape((3,), order='F'))
    prob.constraints[205].save_dual_value(np.array(res.cpg_dual.d205).reshape((3,), order='F'))
    prob.constraints[206].save_dual_value(np.array(res.cpg_dual.d206).reshape((3,), order='F'))
    prob.constraints[207].save_dual_value(np.array(res.cpg_dual.d207).reshape((3,), order='F'))
    prob.constraints[208].save_dual_value(np.array(res.cpg_dual.d208).reshape((3,), order='F'))
    prob.constraints[209].save_dual_value(np.array(res.cpg_dual.d209).reshape((3,), order='F'))
    prob.constraints[210].save_dual_value(np.array(res.cpg_dual.d210).reshape((3,), order='F'))
    prob.constraints[211].save_dual_value(np.array(res.cpg_dual.d211).reshape((3,), order='F'))
    prob.constraints[212].save_dual_value(np.array(res.cpg_dual.d212).reshape((3,), order='F'))
    prob.constraints[213].save_dual_value(np.array(res.cpg_dual.d213).reshape((3,), order='F'))
    prob.constraints[214].save_dual_value(np.array(res.cpg_dual.d214).reshape((3,), order='F'))
    prob.constraints[215].save_dual_value(np.array(res.cpg_dual.d215).reshape((3,), order='F'))
    prob.constraints[216].save_dual_value(np.array(res.cpg_dual.d216).reshape((3,), order='F'))
    prob.constraints[217].save_dual_value(np.array(res.cpg_dual.d217).reshape((3,), order='F'))
    prob.constraints[218].save_dual_value(np.array(res.cpg_dual.d218).reshape((3,), order='F'))
    prob.constraints[219].save_dual_value(np.array(res.cpg_dual.d219).reshape((3,), order='F'))
    prob.constraints[220].save_dual_value(np.array(res.cpg_dual.d220).reshape((3,), order='F'))
    prob.constraints[221].save_dual_value(np.array(res.cpg_dual.d221).reshape((3,), order='F'))
    prob.constraints[222].save_dual_value(np.array(res.cpg_dual.d222).reshape((3,), order='F'))
    prob.constraints[223].save_dual_value(np.array(res.cpg_dual.d223).reshape((3,), order='F'))
    prob.constraints[224].save_dual_value(np.array(res.cpg_dual.d224).reshape((3,), order='F'))
    prob.constraints[225].save_dual_value(np.array(res.cpg_dual.d225).reshape((3,), order='F'))
    prob.constraints[226].save_dual_value(np.array(res.cpg_dual.d226).reshape((3,), order='F'))
    prob.constraints[227].save_dual_value(np.array(res.cpg_dual.d227).reshape((3,), order='F'))
    prob.constraints[228].save_dual_value(np.array(res.cpg_dual.d228).reshape((3,), order='F'))
    prob.constraints[229].save_dual_value(np.array(res.cpg_dual.d229).reshape((3,), order='F'))
    prob.constraints[230].save_dual_value(np.array(res.cpg_dual.d230).reshape((3,), order='F'))
    prob.constraints[231].save_dual_value(np.array(res.cpg_dual.d231).reshape((3,), order='F'))
    prob.constraints[232].save_dual_value(np.array(res.cpg_dual.d232).reshape((3,), order='F'))
    prob.constraints[233].save_dual_value(np.array(res.cpg_dual.d233).reshape((3,), order='F'))
    prob.constraints[234].save_dual_value(np.array(res.cpg_dual.d234).reshape((3,), order='F'))
    prob.constraints[235].save_dual_value(np.array(res.cpg_dual.d235).reshape((3,), order='F'))
    prob.constraints[236].save_dual_value(np.array(res.cpg_dual.d236).reshape((3,), order='F'))
    prob.constraints[237].save_dual_value(np.array(res.cpg_dual.d237).reshape((3,), order='F'))
    prob.constraints[238].save_dual_value(np.array(res.cpg_dual.d238).reshape((3,), order='F'))
    prob.constraints[239].save_dual_value(np.array(res.cpg_dual.d239).reshape((3,), order='F'))
    prob.constraints[240].save_dual_value(np.array(res.cpg_dual.d240).reshape((3,), order='F'))
    prob.constraints[241].save_dual_value(np.array(res.cpg_dual.d241).reshape((3,), order='F'))
    prob.constraints[242].save_dual_value(np.array(res.cpg_dual.d242).reshape((3,), order='F'))
    prob.constraints[243].save_dual_value(np.array(res.cpg_dual.d243).reshape((3,), order='F'))
    prob.constraints[244].save_dual_value(np.array(res.cpg_dual.d244).reshape((3,), order='F'))
    prob.constraints[245].save_dual_value(np.array(res.cpg_dual.d245).reshape((3,), order='F'))
    prob.constraints[246].save_dual_value(np.array(res.cpg_dual.d246).reshape((3,), order='F'))
    prob.constraints[247].save_dual_value(np.array(res.cpg_dual.d247).reshape((3,), order='F'))
    prob.constraints[248].save_dual_value(np.array(res.cpg_dual.d248).reshape((3,), order='F'))
    prob.constraints[249].save_dual_value(np.array(res.cpg_dual.d249).reshape((3,), order='F'))
    prob.constraints[250].save_dual_value(np.array(res.cpg_dual.d250).reshape((3,), order='F'))
    prob.constraints[251].save_dual_value(np.array(res.cpg_dual.d251).reshape((3,), order='F'))
    prob.constraints[252].save_dual_value(np.array(res.cpg_dual.d252).reshape((3,), order='F'))

    # store additional solver information in problem object
    prob._status = res.cpg_info.status
    if abs(res.cpg_info.obj_val) == 1e30:
        prob._value = np.sign(res.cpg_info.obj_val) * np.inf
    else:
        prob._value = res.cpg_info.obj_val
    primal_vars = {var.id: var.value for var in prob.variables()}
    dual_vars = {c.id: c.dual_value for c in prob.constraints}
    solver_specific_stats = {'obj_val': res.cpg_info.obj_val,
                             'status': prob._status,
                             'iter': res.cpg_info.iter,
                             'pri_res': res.cpg_info.pri_res,
                             'dua_res': res.cpg_info.dua_res,
                             'time': res.cpg_info.time}
    attr = {'solve_time': t1 - t0, 'solver_specific_stats': solver_specific_stats, 'num_iters': res.cpg_info.iter}
    prob._solution = Solution(prob.status, prob.value, primal_vars, dual_vars, attr)
    results_dict = {'solver_specific_stats': solver_specific_stats,
                    'num_iters': res.cpg_info.iter,
                    'solve_time': t1 - t0}
    prob._solver_stats = SolverStats.from_dict(results_dict, 'SCS')

    return prob.value


